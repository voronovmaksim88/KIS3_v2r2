version: '3.8'

networks:
  dev:

services:
  nginx:
    build:
      context: .
      dockerfile: Dockerfile.nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
    command: >
      sh -c "fullchain=$(find /etc/letsencrypt/archive/sibplc-kis3.ru -name 'fullchain*.pem' | sort -V | tail -n 1) &&
             privkey=$(find /etc/letsencrypt/archive/sibplc-kis3.ru -name 'privkey*.pem' | sort -V | tail -n 1) &&
             cp $fullchain /etc/nginx/ssl/fullchain.pem &&
             cp $privkey /etc/nginx/ssl/privkey.pem &&
             chmod 644 /etc/nginx/ssl/fullchain.pem &&
             chmod 600 /etc/nginx/ssl/privkey.pem &&
             nginx -g 'daemon off;'"
    depends_on:
      - backend
    networks:
      - dev

  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"
    networks:
      - dev